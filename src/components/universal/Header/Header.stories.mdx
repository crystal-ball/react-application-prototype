import { Canvas, Meta, Story } from '@storybook/addon-docs'
import { storeDecorator } from '@/utils/storybook'
import { Header } from './Header'

<Meta title='App/Header' component={Header} decorators={[storeDecorator()]} />

# Header

The header component will only render outside the landing screen. _(The application will
render a hero element on the landing screen)_

<Canvas>
  <Story name='<Header />'>
    <Header />
  </Story>
</Canvas>
